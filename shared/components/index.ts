/**
 * Shared Components
 * 
 * React components that could be shared (if using a monorepo build setup)
 * 
 * NOTE: Currently, frontend components remain in frontend/src/components
 * This folder is for components that are truly shared or could be shared
 */

// Add shared components here as needed
// Example:
// export { SharedComponent } from './SharedComponent';

